<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jtrofe200.myapplication" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".onlineObjects.ProfileActivity"
            android:label="@string/act_name_profile" />
        <activity
            android:name=".onlineObjects.OnlineActivity"
            android:label="@string/title_activity_online" />
        <activity
            android:name=".onlineObjects.NewGameActivity"
            android:label="@string/title_activity_new_game" />
        <activity
            android:name=".onlineObjects.AddOnActivity"
            android:label="@string/title_activity_add_on" >
        </activity>
    </application>

</manifest>
